<ion-view view-title="Playlists">
  <ion-content>
    <ion-list>
      <ion-item ng-repeat="playlist in playlists" href="#/app/playlists/{{playlist.id}}">
        {{playlist.id}} - {{playlist.first_name | uppercase}} {{playlist.last_name | uppercase}} 
      </ion-item>
    </ion-list>
  </ion-content>
</ion-view>

